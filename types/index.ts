export type Social = { id: number; icon: string; path: string };

export type SocialsList = Social[];

export type Project = {
  id: number;
  title: string;
};

export type ProjectsList = Project[];
